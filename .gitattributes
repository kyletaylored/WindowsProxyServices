# Auto detect text files and perform LF normalization
* text=auto

# Force CRLF for Windows batch files
*.bat text eol=crlf
*.cmd text eol=crlf

# Keep PowerShell scripts with LF (cross-platform compat)
*.ps1 text eol=lf

# Binary files
*.png binary
*.jpg binary
*.exe binary
*.dll binary
